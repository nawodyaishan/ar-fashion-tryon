# Core API dependencies
fastapi
uvicorn[standard]
python-multipart
pillow
gunicorn
cloudinary
requests
python-dotenv

# Image processing
numpy<2.0.0  # Pin to avoid binary incompatibility with MMPose
rembg
onnxruntime
opencv-python-headless

# Virtual try-on (Gradio API)
gradio-client>=1.4.0

# TensorFlow for garment classification
tensorflow-cpu

# MMPose for garment keypoint detection
openmim
mmengine>=0.8.0,<0.11.0  # Pin to avoid NumPy 2.x requirement
# Note: mmcv will be installed separately (see install_mmpose.sh)
mmpose>=1.0.0,<1.4.0  # Pin to compatible version
mmdet>=3.0.0,<3.4.0  # Pin to compatible version
